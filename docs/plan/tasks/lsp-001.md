---
id: lsp-001
scope: tools/code-intel (D-012 item 2; repo navigation owner)
status: implemented
priority: P1
---

# objective

Freeze a **docs-first** LSP-backed `code_intel` tool contract: one model Tool
(`code_intel`) with subcommand-style ops (`hover` / `definition` / `references`
/ `diagnostics`) backed by a persistent per-workspace Language Server session
(zig → **zls**, v1), returning **text tool results** — no UI overlays, no inlay
hints, no multi-root, no multi-language in v1. The LSP client stays **outside
`zag-agent-core`** (D-012 architecture law) and is small enough to implement in
one engineer slice (~1–2 days): JSON-RPC framing + a minimal LSP message
subset, ~400–700 lines total.

**Binding specification:** [lsp.md](../../modules/lsp.md)

**Status:** **`implemented`** — see the implementation log at the bottom.

# status truth

| Track | Status |
|-------|--------|
| Contract | **implemented** — Wave 2 closeout 2026-08-14: [architecture](../reviews/lsp-001-01-architecture.md) PASS · [safety](../reviews/lsp-001-02-safety.md) PASS; env allow-list deferred (module §12 Q2; closes on long-lived slots) |
| Implementation | **done** — see commit line at the bottom |
| Maturity | **unchanged** — no maturity row raise; new coding-agent capability, L2 discipline |
| Depends-on | **none required** (see §dependency decision) |
| Unblocks | repo-navigation slice, richer hover/definition UX, later multi-language rows |

# context

- D-012 item 2: "repository navigation and LSP-backed code intelligence".
  D-012 also states the **architecture law**: LSP stays outside `zag-agent-core`.
- Reference (semantic only, **not** parity — D-009): hyper-grok-build TUI wires
  LSP via tower-lsp clients per workspace; hover/definitions/diagnostics are
  surfaced to the agent as **tool results**.
- Roadmap D-012 route lists LSP "after supervisor". **Dependency decision**
  (frozen in this contract): lsp-001 **stands alone** with a minimal
  spawn/cleanup contract, because `process-supervisor-001` v1 is a
  **foreground-bounded** supervisor (mandatory deadline, `background jobs out
  of v1`, bounded-capture pump) — it cannot host a **long-lived bidirectional
  stdio** LSP server. lsp-001 therefore owns its child via Zig 0.16
  `std.process.spawn` (stdio pipes) and kills it on Agent deinit / idle timeout.
  When the supervisor later gains long-lived slots, the LSP child **migrates**
  to a supervised slot per [process-supervisor.md §2](../../modules/process-supervisor.md);
  this contract keeps the migration mechanical (spawn/kill are the only
  supervisor-shaped surfaces).
- Host state (2026-08-06, this machine): `zls` **not installed**; `rust-analyzer`
  1.97.1 (`~/.cargo/bin`), `clangd` (`/usr/bin`), `tsserver` (`/opt/homebrew/bin`)
  present. v1 server table = **zig → zls only**; the table shape admits later
  rows. The frozen fallback — server not found → tool body `null` — is the
  designed behavior on this host until zls is installed.
- Ecosystem finding: no LSP client is vendored in zag or pi-mono-zig
  (pi-mono-zig has MCP JSON-RPC over stdio as a reference only). zls ships a
  generated `src/lsp.zig` (MIT) from the LSP meta model; it is the full 3.17
  model (~20k generated lines), heavier than this slice needs. **Decision: a
  hand-rolled minimal client** (all new MIT-clean code); vendoring zls's
  `lsp.zig` later is an option if the surface grows (semantic tokens, inlay).
- Zag is on **Zig 0.16** (`std.Io` era): `std.process.spawn(io, .{ .argv,
  .cwd, .stdio = .{ .stdin = .pipe, .stdout = .pipe, .stderr = ... } })` →
  `Child` with parent pipe ends; `child.kill(io)` / `child.wait(io)` (0.16 has
  no `terminate` — SIGTERM is sent via `std.posix.kill`); `std.Io.sleep(io,
  duration, clock)` for poll pacing.

# path

| Path | Role |
|------|------|
| `docs/modules/lsp.md` | **binding** (frozen contract) |
| `docs/plan/tasks/lsp-001.md` | this task |
| Future reviews | `docs/plan/reviews/lsp-001-01-*.md` |
| Impl (this slice) | `packages/zag-coding-agent/src/runtime/code_intel_tool.zig` + `runtime/lsp/jsonrpc.zig` + `runtime/lsp/client.zig`; wiring in `toolset.zig`, `agent.zig`; fixture under `tests/` |
| `docs/modules/README.md` | add `lsp.md` row (deferred to contract-freeze housekeeping; not this slice) |
| Forbidden | product/build/CI; Core; new package without ownership decision |

# dependency decision (frozen)

| Question | Decision |
|----------|----------|
| Does lsp-001 block on process-supervisor-001? | **No.** Supervisor v1 is foreground-bounded (mandatory deadline, no background jobs, bounded-capture pump) — incompatible with a long-lived stdio JSON-RPC server. |
| Standalone contract? | **Yes** — minimal spawn/cleanup: `spawn` (stdio pipes) → pump with deadline → on end: graceful `shutdown`/`exit` → SIGTERM → bounded wait (2 s) → SIGKILL → `wait`. Direct-child only; **no** process-tree claims. |
| Migration | When the supervisor gains long-lived slots, the LSP child registers as a supervised slot (process-supervisor §2 lists LSP as a future slot). Only `spawn`/`kill` surfaces change. |
| Shared principles | Adopt supervisor principles where they fit: truthful terminal per session, bounded memory (caps in binding §7), no OS-sandbox claims. |

# contract summary (implemented)

| Topic | Freeze |
|-------|--------|
| Owner | `zag-coding-agent` only; **no** Core ports, no Session/Trace/headless schema fields |
| Surface | One model Tool `code_intel`; ops `hover` \| `definition` \| `references` \| `diagnostics`; text results only |
| Wire format | `{"op": "...", "path": "./src/x.zig", "line": 12, "col": 3}` — `line`/`col` **0-based** (LSP convention), required for hover/definition/references; diagnostics ignores them |
| Session lifecycle | Lazy start on first tool call (per workspace root); kill on `Agent.deinit` (app exit); idle timeout **10 min** (teardown + restart on next call); restart after server crash |
| Server table | v1: `zig → ["zls"]`; discovered via PATH; config table admits later languages but **no** multi-language in v1 |
| Fallback | Server not found / spawn failure / unresolved workspace root → tool body **`null`** (exact 4 bytes) |
| Result format | Success: `intel-v1: op=<op> status=ok` first line + content; no-result: `null`; errors: `error: code=<code> message=<human>` (core codes + `too_large`) |
| Deadlines | Session startup (initialize handshake) ≤ **20 s**; per-op request ≤ **15 s**; POSIX non-blocking pipe poll (20 ms granularity) |
| Budgets | File read 4 MiB; response message ≤ 8 MiB; hover text ≤ 32 KiB; references ≤ 50 hits; diagnostics ≤ 200 entries / 32 KiB per URI; open-file cache ≤ 64 URIs; stderr ring ≤ 8 KiB |
| Position encoding | Negotiate `positionEncoding`; client declares `["utf-8"]`; fallback utf-16 per server capability (byte→UTF-16 unit conversion implemented) |
| Descriptor | `risk=read`, `workspace.path_field="path"`, `cancellation=none`, `shell=none` |
| Fixtures | Mock LSP server (Python stdlib, deterministic) over real pipes; private `builtin.is_test` seam overrides server binary/deadlines/idle |
| Maturity | **no row raise**; no OS sandbox; direct-child cleanup only |
| Orthogonal | TUI / Theme / vaxis; process-supervisor; rpc-v1 / ACP; MCP / E2; edit-transaction semantics |

# verification (implementation track — done)

Deterministic evidence comes from the **mock-server fixture classes** (no zls
required). Real-zls smoke is a later step on a host with zls installed (absent
on this machine as of 2026-08-06).

| # | Class | Status |
|---|-------|--------|
| 1 | Happy hover | ✅ markdown contents; `intel-v1` first line + text; initialize handshake + didOpen logged |
| 2 | Definition | ✅ location `<path>:<line+1>:<col+1>` + in-workspace source line |
| 3 | References | ✅ 2 hits; truncation beyond 50 with `...[truncated N more]` |
| 4 | Diagnostics | ✅ publish after didOpen; pulled by `diagnostics` op; second call instant (cache) |
| 5 | Server not found | ✅ PATH miss → body exactly `null` |
| 6 | Server crash mid-request | ✅ bounded `tool_failed` + stderr tail; next call restarts (new PID) |
| 7 | Response timeout | ✅ no hang; bounded `tool_failed`; session torn down; next call restarts |
| 8 | Idle timeout | ✅ child killed after idle (exit logged); next call restarts (new PID) |
| 9 | Jail escape | ✅ `../` / absolute → `jail_deny`; no spawn, no log file |
| 10 | Invalid args / unknown op / negative line | ✅ `invalid_arguments` (6 cases) |
| 11 | File over 4 MiB | ✅ `too_large`; no didOpen, no spawn |
| 12 | Ownership | ✅ no `zag-agent-core` symbols in the LSP modules; all sources under `zag-coding-agent` |
| 13 | Budgets | ✅ message > 8 MiB → protocol error; hover > 32 KiB → marker; refs > 50 → marker; diags > 200 → marker (finite memory) |
| 14 | Toolset regression | ✅ Phase1Storage descriptor test [10]→[11]; coding-agent suite green (468/468) |
| 15 | Empty answers | ✅ hover/definition/references empty result → `null`; diagnostics cache empty after wait → `null` |
| 16 | Position mapping | ✅ mock echoes the received 0-based position; `pos=2:3` asserted |
| 17 | Multi-root isolation | ✅ one session per workspace root; roots do not share sessions (PIDs differ, no restart) |
| 18 | Redaction | ✅ code_intel tool args flow through the trace redaction pipeline (secret → marker) |
| 19 | Agent.deinit kill | ✅ state deinit tears down the child; graceful `exit` logged; PID reaped |
| 20 | Unresolved root / missing file | ✅ `null`, no spawn |

# non-goals

- UI overlays, hover popups, editor integration, inlay hints, semantic tokens
- Multi-root workspace; multi-language server rows in v1
- rename / codeAction / completion / signatureHelp / documentSymbol
- Background indexing service or project-wide diagnostics broadcast
- Process-tree ownership, OS sandbox, PTY
- Blocking on process-supervisor-001 (see §dependency decision)
- Session/Trace/headless schema changes; Core ports; maturity raise

# implementation log

- `runtime/lsp/jsonrpc.zig`: hand-rolled JSON-RPC 2.0 framing — LSP
  `Content-Length` header framing, streaming decoder (header line ≤ 4 KiB,
  message ≤ 8 MiB, strict blank-separator handling across chunk boundaries),
  message classification (request/response/notification), owned parsed-tree
  transfer, JSON escaping. ~230 lines.
- `runtime/lsp/client.zig`: minimal LSP session — spawn (stdio pipes,
  non-blocking poll loop, 20 ms pacing), `initialize` handshake (≤ 20 s,
  positionEncoding negotiation incl. utf-8→utf-16 unit conversion),
  `initialized`, didOpen/didChange full-text sync with versioning + 64-URI
  cache, publishDiagnostics cache (≤ 200 entries / 32 KiB per URI,
  workspace-relative display), request/response correlation by id, unknown
  server requests answered `-32601`, idle timeout (10 min), teardown
  (`shutdown` ≤ 1 s → `exit` → voluntary-exit wait → SIGTERM → bounded wait →
  SIGKILL → reap; direct child only), bounded stderr ring (8 KiB) surfaced on
  `tool_failed`. ~560 lines.
- `runtime/code_intel_tool.zig`: model tool `code_intel` — descriptor
  (risk=read, path_field, cancellation=none, shell=none), arg validation
  (op enum, 0-based line/col, negative/non-integer → `invalid_arguments`),
  Guard containment before any server interaction, 4 MiB read budget
  (`too_large`), per-workspace-root session cache with idle/crash restart,
  result formatting per §3.3/§3.4 (hover join + 32 KiB cap, definition ≤ 16
  locations with in-workspace source lines, references ≤ 50 hits, hard 64 KiB
  result cap), exact-`null` fallback, private `builtin.is_test` seam
  (server binary/argv/deadlines/idle/teardown-grace; never in Tool JSON,
  Agent.Options, CLI, or session schema).
- Wiring: `toolset.zig` Phase1Storage [10]→[11] (`code_intel` appended,
  descriptor test updated); `agent.zig` owns a heap-stable `CodeIntelState`
  (created in init, torn down in deinit — kills live server children);
  `root.zig` re-exports the public surface.
- Fixture: `tests/mock_lsp_server.py` — Python 3 stdlib-only deterministic
  LSP mock over real pipes, driven by a JSON config (hover/definition/
  references shapes, diagnostics counts/delays, crash-after-N, response
  delays, ignore-methods, huge-hover/huge-message, empty answers, per-event
  log file).
- **Env allow-list note**: the binding §5.1 allow-list env
  {PATH, HOME, LANG, LC_ALL, LC_CTYPE, ZLS_*} is **not** enforced in v1 —
  the client spawns with `environ_map = null` (inherits the parent env,
  same as `run_shell`). Zig 0.16 exposes no libc-free parent-environ read
  and the module's fixture list has no allow-list gate; module §12 Q2 stays
  open as a hardening TODO.
- Verified: `zig build test` in `packages/zag-coding-agent` — **465/465**
  green (includes 20 lsp-001 fixture classes + jsonrpc/client unit tests);
  the repo-root coding artifact — **468/468** green when the test binary is
  run directly. NOTE: the 4-matrix `zig build test` runs currently fail in
  this environment for reasons **independent of lsp-001**: the same 4 test
  artifacts (openai/ai/cli/coding) fail under the build runner's `--listen`
  mode and the TUI PTY gates report 8/14 at **HEAD~1 (pre-lsp baseline)
  identically**; every artifact's test binary passes all tests when run
  directly. This is an environment regression since the last green matrix
  run (ac95de3) and is tracked separately from this slice.

commit: 75f213b "lsp-001: code_intel tool — hover/definition/references/diagnostics via a hand-rolled LSP client" (code; doc status + closeout follow)

# related

- [lsp.md](../../modules/lsp.md) (binding)
- [process-supervisor-001](./process-supervisor-001.md) · [process-supervisor.md](../../modules/process-supervisor.md)
- [edit-transaction-001](./edit-transaction-001.md) · [tools-edit.md](../../modules/tools-edit.md) · [tools-shell.md](../../modules/tools-shell.md)
- [D-012](../../decisions/active/D-012-complete-local-coding-agent-target.md) · [D-011](../../decisions/active/D-011-thin-agent-core-boundary.md) · [D-009](../../decisions/active/D-009-pi-semantics-not-parity-fork.md)
- [2026-08-06 analysis](../analysis/2026-08-06-pi-omp-hyper-local-agent-analysis.md)
